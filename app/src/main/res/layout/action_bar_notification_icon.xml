<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="fill_parent"
    android:layout_gravity="center" android:gravity="center"
    android:clickable="true"
    style="@android:style/Widget.ActionButton"
    >
    <ImageView
        android:id="@+id/item_imgView"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:src="@drawable/shopping_cart"
        android:layout_margin="0dp"
        />
    <TextView
        android:id="@+id/item_txtView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:minWidth="17sp" android:textSize="12sp" android:textColor="#ffffffff"
        android:layout_marginTop="3dp" android:layout_marginRight="0dp"
        android:paddingBottom="1dp" android:paddingRight="4dp" android:paddingLeft="4dp"
        android:background="@drawable/rounded_square"
        android:gravity="center"/>
</RelativeLayout>